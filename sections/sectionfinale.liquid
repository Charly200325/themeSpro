<style>
.skin-feature-section {
  position: relative;
  width: 100%;
  overflow: hidden;
  background-color: #fdf2e8;
  margin-top: -30px; /* supprime l'espace vide */
}
.skin-feature-image {
  width: 100%;
  display: block;
}
.elvynor-caption {
  position: absolute;
  top: 45%;
  left: 50%;
  transform: translate(-50%, -50%);
  text-align: center;
  color: #60362C;
  font-family: 'Hanken Grotesk', sans-serif;
}
.elvynor-caption p {
  font-size: 18px;
  margin: 0;
}
.elvynor-caption h2 {
  font-size: 32px;
  font-weight: 600;
  margin: 0;
  line-height: 1.2;
}
.elvynor-caption h2::after {
  content: "But Better";
  display: block;
}
.skin-feature-overlay {
  position: absolute;
  bottom: 40px;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  flex-wrap: nowrap;
  gap: 20px;
  justify-content: center;
  padding: 20px;
  max-width: 1200px;
  width: 100%;
  background-color: transparent;
}
.skin-feature-card {
  background: rgba(255, 255, 255, 0.85);
  border-radius: 12px;
  padding: 20px;
  width: 170px;
  text-align: center;
  font-size: 14px;
  color: #60362C;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
}
.skin-feature-card img {
  width: 36px;
  height: auto;
  margin-bottom: 10px;
}
@media (max-width: 768px) {
  .skin-feature-overlay {
    position: static;
    transform: none;
    padding: 40px 20px;
    background-color: #907B6E;
    flex-wrap: wrap; /* restore wrap on mobile */
  }
  .skin-feature-card {
    width: 45%;
    color: white;
    background: transparent;
    box-shadow: none;
  }
  .skin-feature-card img {
    filter: brightness(0) invert(1);
  }
  .elvynor-caption {
    position: static;
    transform: none;
    margin-top: 20px;
    margin-bottom: 20px;
    color: #60362C;
  }
  .elvynor-caption h2 {
    font-size: 24px;
  }
  .elvynor-caption h2::after {
    content: "But Better";
    display: block;
  }
}
</style>
